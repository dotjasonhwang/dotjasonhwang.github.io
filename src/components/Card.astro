---
const { glowingBorder } = Astro.props;
import "../styles/global.css";
---

<!-- https://ibelick.com/blog/create-animated-gradient-borders-with-css -->
<style>
  .card {
    width: w-full;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: left;
    padding: 20px;
    border: 2px solid transparent;
    border-radius: 16px;
  }

  .glowingBorder {
    background:
      linear-gradient(#222, #222) padding-box,
      linear-gradient(var(--angle), #4cea7f, #222, #222) border-box;
    animation: 8s rotate linear infinite;
  }

  @keyframes rotate {
    to {
      --angle: 360deg;
    }
  }

  @property --angle {
    syntax: "<angle>";
    initial-value: 0deg;
    inherits: false;
  }

  @keyframes fadeInAnimation {
    0% {
      opacity: 0;
    }

    100% {
      opacity: 1;
    }
  }
</style>

<div
  class:list={[
    "card",
    "w-full",
    "max-w-md",
    glowingBorder ? "glowingBorder" : "",
  ]}
>
  <slot />
</div>
